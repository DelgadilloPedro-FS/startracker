{% extends "layouts/base.html.twig" %}

{% block app %}
    <div class="flex-col align-items-center">
        <h1 class="text-center">List of Galaxies</h1>
        <a href="/galaxies/create" class="inline-flex items-center px-4 py-2 bg-green-500 text-white hover:bg-green-700 rounded-md">
            <button type="button">Add New Galaxy</button>
        </a>
    </div>
    {% for galaxy in galaxies %}
        {% include 'partials/_galaxy.html.twig' with galaxy %}
    {% endfor %}
{% endblock %}